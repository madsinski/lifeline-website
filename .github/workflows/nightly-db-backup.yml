name: Nightly DB Backup

# Belt-and-braces backup that runs alongside Supabase's built-in daily
# snapshots (which are 7 day rolling, recoverable only from the
# Supabase dashboard). This dumps the entire public schema + data via
# pg_dump and uploads it to the private "db-backups" Storage bucket.
# Cheap to run, easy to download manually if Supabase is ever
# unreachable, and gives us a portable export if we ever migrate off.
#
# Required GitHub repository secrets (Settings → Secrets → Actions):
#   SUPABASE_DB_URL        — postgres connection string from
#                            Supabase Dashboard → Project → Settings →
#                            Database → "Connection string (URI)".
#                            Use the DIRECT connection (port 5432),
#                            NOT the pooler (6543) — pg_dump needs
#                            session-level connections.
#   SUPABASE_PROJECT_REF   — "cfnibfxzltxiriqxvvru" for this project.
#   SUPABASE_SERVICE_ROLE_KEY — service-role API key (Settings →
#                              API → service_role secret).
#
# Retention: keeps the last 14 daily dumps, prunes anything older.

on:
  schedule:
    # 03:00 UTC = 03:00 in Iceland (UTC year-round). Off-peak.
    - cron: '0 3 * * *'
  workflow_dispatch:    # manual trigger for testing / pre-migration snapshots

permissions:
  contents: read

concurrency:
  group: nightly-db-backup
  cancel-in-progress: false

jobs:
  backup:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Install postgres client (pg_dump 17)
        run: |
          # Supabase runs Postgres 17, and pg_dump refuses to dump a
          # newer server with an older client. Install the 17 client
          # from PGDG.
          set -euxo pipefail
          sudo install -d -m 0755 /etc/apt/keyrings
          curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc \
            | sudo gpg --dearmor -o /etc/apt/keyrings/pgdg.gpg
          CODENAME=$(lsb_release -cs)
          echo "deb [signed-by=/etc/apt/keyrings/pgdg.gpg] https://apt.postgresql.org/pub/repos/apt ${CODENAME}-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list > /dev/null
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17
          echo "/usr/lib/postgresql/17/bin" >> "$GITHUB_PATH"
          /usr/lib/postgresql/17/bin/pg_dump --version

      - name: Dump database
        id: dump
        env:
          DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          set -euo pipefail
          if [ -z "${DB_URL:-}" ]; then
            echo "::error::SUPABASE_DB_URL secret is missing or empty."
            echo "Check: Repo → Settings → Secrets and variables → Actions →"
            echo "       Repository secrets → SUPABASE_DB_URL exists."
            exit 1
          fi
          # Also sanity-check the URL shape so we fail fast with a clear
          # message if someone pasted a placeholder. Don't log the URL.
          case "$DB_URL" in
            postgresql://*|postgres://*) : ;;
            *) echo "::error::DB_URL does not start with postgresql:// — fix the secret value."; exit 1 ;;
          esac
          if printf %s "$DB_URL" | grep -q "hidden\|YOUR-PASSWORD\|\[" ; then
            echo "::error::DB_URL still contains a placeholder ('hidden', '[YOUR-PASSWORD]', etc). Paste the revealed Session-pooler URL from Supabase."
            exit 1
          fi
          DATE=$(date -u +%Y-%m-%d-%H%M)
          FILENAME="lifeline-${DATE}.sql.gz"
          # --no-owner / --no-acl: portable across environments — strip
          # the supabase-internal role grants so the dump can be
          # restored to a fresh project without complaints.
          pg_dump \
            --no-owner --no-acl \
            --exclude-schema=auth \
            --exclude-schema=storage \
            --exclude-schema=realtime \
            --exclude-schema=supabase_functions \
            --exclude-schema=vault \
            --exclude-schema=pgsodium \
            --exclude-schema=graphql \
            --exclude-schema=graphql_public \
            --exclude-schema=net \
            --exclude-schema=extensions \
            "$DB_URL" \
            | gzip -9 > "$FILENAME"
          ls -lh "$FILENAME"
          echo "filename=$FILENAME" >> "$GITHUB_OUTPUT"

      - name: Upload to Supabase Storage
        env:
          PROJECT_REF:   ${{ secrets.SUPABASE_PROJECT_REF }}
          SERVICE_KEY:   ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          FILENAME:      ${{ steps.dump.outputs.filename }}
        run: |
          set -euo pipefail
          # PUT + x-upsert lets us safely re-run in the same minute (or
          # retry an earlier failed run) without colliding on filename.
          # POST returns 400/409 on collision; PUT-with-upsert overwrites.
          # Capture response body so we can see the real error if it 4xxs.
          HTTP_CODE=$(curl --show-error -sS -o /tmp/upload-response.json -w "%{http_code}" \
            -X PUT "https://${PROJECT_REF}.supabase.co/storage/v1/object/db-backups/${FILENAME}" \
            -H "Authorization: Bearer ${SERVICE_KEY}" \
            -H "apikey: ${SERVICE_KEY}" \
            -H "Content-Type: application/octet-stream" \
            -H "x-upsert: true" \
            --data-binary "@${FILENAME}")
          echo "Status: ${HTTP_CODE}"
          echo "Body:"
          cat /tmp/upload-response.json
          echo
          if [ "${HTTP_CODE}" != "200" ] && [ "${HTTP_CODE}" != "201" ]; then
            echo "::error::Upload failed (HTTP ${HTTP_CODE})"
            exit 1
          fi
          echo "Uploaded: ${FILENAME}"

      - name: Prune old backups (keep newest 14)
        env:
          PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -euo pipefail
          # List all objects in the bucket, sorted newest-first. Don't
          # --fail so we see the actual response body if anything 4xxs.
          HTTP_CODE=$(curl --show-error -sS -o /tmp/list.json -w "%{http_code}" \
            -X POST "https://${PROJECT_REF}.supabase.co/storage/v1/object/list/db-backups" \
            -H "Authorization: Bearer ${SERVICE_KEY}" \
            -H "apikey: ${SERVICE_KEY}" \
            -H "Content-Type: application/json" \
            --data '{"prefix": "", "limit": 500, "sortBy": {"column": "created_at", "order": "desc"}}')
          echo "List status: ${HTTP_CODE}"
          echo "List body:"
          cat /tmp/list.json
          echo
          if [ "${HTTP_CODE}" != "200" ]; then
            echo "::warning::Prune list failed (HTTP ${HTTP_CODE}) — skipping prune. Backup itself succeeded."
            exit 0
          fi
          python3 - <<'PY'
          import json, urllib.request, os
          items = json.load(open('/tmp/list.json'))
          # Keep newest 14, delete the rest.
          to_delete = [i['name'] for i in items[14:]]
          if not to_delete:
              print("No old backups to prune (have", len(items), "total).")
              raise SystemExit(0)
          ref = os.environ['PROJECT_REF']
          key = os.environ['SERVICE_KEY']
          for name in to_delete:
              url = f"https://{ref}.supabase.co/storage/v1/object/db-backups/{name}"
              req = urllib.request.Request(url, method='DELETE',
                  headers={'Authorization': f'Bearer {key}', 'apikey': key})
              try:
                  urllib.request.urlopen(req).read()
                  print("deleted", name)
              except Exception as e:
                  print("could not delete", name, ":", e)
          PY
